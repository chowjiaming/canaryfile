{
  "name": "canaryfile",
  "version": "0.1.0",
  "description": "Snapshot testing for your AI coding agent. Know when a model upgrade, CLAUDE.md edit, or MCP change breaks your workflow before you merge it.",
  "type": "module",
  "bin": {
    "canaryfile": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "tsup": "^8.5.1",
    "typescript-eslint": "^8.67.0"
  }
}
